# Phase 1 Scope

Implemented in the scaffold:

- FastAPI app startup
- Minimal browser UI
- Audio device listing
- Session start, stop, pause, resume API shape
- Transcript segment model with reserved `speaker` field
- Markdown default output writer
- Optional TXT export writer

Placeholder or partial in the scaffold:

- Real microphone capture
- Real Windows loopback capture
- Real faster-whisper transcription pipeline
- Full pause and resume semantics for live audio pipelines
